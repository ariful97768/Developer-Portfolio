import React from 'react';

const ShiningBoldTxt = ({text}) => {
    return (
        <span className='text-white px-3 rounded-xl bg-black'>
            {text}
        </span>
    );
};

export default ShiningBoldTxt;